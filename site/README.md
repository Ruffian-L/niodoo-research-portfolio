# Field face

Renders this git. Does not invent a new layout.

Open `index.html` in a browser, or:

```
python3 -m http.server 8765 --directory site
```

HIRE is the default: plaque, three live receipts, Splatrag, trail shaded, contamination as a hole. LAB lights threads and dated names. PATH is the changelog spine.

INSPECT is the silo card schema: result / what to take / where / silo.
