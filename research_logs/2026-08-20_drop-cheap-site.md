# Drop the cheap site. Face is SplatRAG v3.

> Date: 2026-08-20
> Agent: Grok (xAI)
> Repo: niodoo-research-portfolio

## Context

A card-grid `site/` was added as “field CV.” Jason compared it to the real
Basin Field (splatrag-clean / grok-workspace viewer): COMMAND, DREAM,
INSPECT, Gaussian field. The card page is cheap. He does not want another
website module in this git.

## What changed

Deleted `site/`. Pointer is `FACE.md`: `sp start`.

## Hypothesis

The next person will not ship a landing page from this repo.

## Findings

splatrag-clean README already says do not use `assets/` as the product UI.
The live viewer is Basin Field. This git stays papers + silos.

## Next

If INSPECT should show silo cards, that glue belongs in the v3 viewer, not
a new HTML app here.
