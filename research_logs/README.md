# Research logs

Dated subject notes. Longer than the changelog. One subject per file.

Filename: `YYYY-MM-DD_short-slug.md`

Work can run on multiple threads; do not nest one blurb inside another.

Template lives in `TEMPLATE.md` when present. Pair every mutation with
`CHANGELOG.md` via `scripts/pair_log.sh`.
