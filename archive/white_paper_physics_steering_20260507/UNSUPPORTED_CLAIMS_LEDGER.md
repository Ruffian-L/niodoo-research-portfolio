# Unsupported / Rewrite Ledger

This file is the removal/refinement ledger requested for claims that were not cleanly backed after the artifact sweep.

| Draft claim | Problem found | Action |
|---|---|---|
| `force_cap = 35` as the current universal setting. | `35` is strongly supported as a historical stabilization from `80`, but current copied defaults/configs show `7.5` and `5.5`. | Rewrite as a historical calibration and add chronology. |
| Force-cap delta telemetry as proof of `force_cap = 35`. | One deep-dive ties specific hidden-state delta metrics to `35`, but the cited telemetry log header shows `force_cap:80.0`. | Keep the metrics only if tied to the actual logged config; do not use them as proof of the `35` cap. |
| M9/TEDE `baseline_60` as a clean broad success. | `M9_COMPLETION_GUIDE.md` reports `80%`, but `final_basin_assessment.json` marks `baseline_60` not mint-ready and `tede_eval_result.json` selects a different best candidate. | State only that one M9 guide reports the result; treat broad TEDE completion as conflicted. |
| Exact asymmetry row `negative growth -0.3 vs positive +0.5`. | Second pass found the implemented pair in older MountainCar momentum snapshots. The first-pass downgrade was too conservative. | Keep the row, but call these growth increments under a symmetric cap, not "growth caps." |
| Asymmetry constant `1.15-1.30` across all three subsystems. | `1.15`, `0.7`, and the `-0.3/+0.5` flux pair are real, but they do not all reduce to the same scalar range. | Upgrade wording: the invariant is recurring negative-signal asymmetry, not one universal scalar constant. |
| Negative ghost on `car` produces semantic antipode. | Found as a proposed Block/Bridge plan, not as an executed run. | Replace with executed Glub-Tub/Glub-Glub/Magma-Eating-Hamster examples. |
| Team-build `anti_ghost_vectors` prove active anti-ghost injection. | The fields and add methods exist, but the inspected `inject_ghost_sequence` function is a no-op. | Cite as representation/channel evidence only unless another active injection path is manually verified. |
| `Magma-Eating-Hamster` negative steering cleanly produced `Bathtub` or `Heater`. | This appears in prose summaries, but the line-addressable run extract found in this package supports narrower negative-gain drift toward refusal/inanimate concepts such as `firebrick`. | Use the line-addressable Glub/firebrick evidence in the draft; keep stronger examples as prose-only leads until raw run output is located. |
| VR-H1 persistence threshold "near `1.05`" as active runtime threshold. | `1.05` appears in detector documentation; active `main.rs` passes `2.0`. | Say documented threshold example is `1.05`; active wiring currently uses `2.0`. |
| Micro-dream cadence from config. | Config intervals exist, but active `main.rs` runs the micro-dream path every step after a step threshold. | Cite active runtime behavior separately from config/TUI behavior. |
| TopoCoT injects reflection tokens or text into the active generation stream. | Reflection fields are defined in `dream.rs`, but active `main.rs` only uses the corrected tensor. No downstream listener for `reflection_text`, `reflection_tokens`, or `topocot_threshold` was found. | Rewrite as hydraulic-jump flagging/correction only; mark token-stream reflection as incomplete integration. |
| Historical dream correction threshold `5.0`. | Manual review found current copied code/config support `6.0`; no clean package artifact supports `5.0` as a dream threshold. | Remove the `5.0` claim unless a specific artifact is later found. |
| Persistent cross-session hydrodynamic memory. | Research logs discuss persistence; active `main.rs` notes persistence disabled. | Limit to splat memory mechanics unless citing a specific persisted run artifact. |
| PARB benchmark improvement. | Both headline directions depend on machine fields. The positive rescore favors Niodoo but manual row checks found baseline false negatives caused by terminal escape-code parsing. The later Grok-judged multi-seed file favors baseline but contains judge/API failures and visible rows needing human review. | Keep PARB as contested benchmark evidence with manually observed positive and negative examples; do not claim either clean loss or clean win until row-level manual adjudication is complete. |
| Three-zigzag thermostat as a built mechanism. | Component signals exist; explicit phase coupling was not found. | Keep only as roadmap/high-leverage integration hypothesis. |
| Route-memory codec behavior improvement. | Route preservation is strong, but runtime causal reviews show weak visible-output selectivity. | Distinguish route preservation from behavior control. |
| Niodoo proves baseline LLMs universally lack curiosity-before-dismissal. | Niodoo has explicit `EXPLORE`/`SPIKE`/`FOCUS` controls and MountainCar has a curiosity/void precursor, but no copied benchmark directly compares this primitive against baseline LLM rejection behavior. | Present as a research thesis and benchmark target, not a completed comparative claim. |

No claim was removed from the revised draft solely because it sounded ambitious. Claims were downgraded only when copied artifacts contradicted them or no executed evidence was found.
